# Async Push Scripts

## Usage

```bash
# Push in background (non-blocking)
git pushasync
git pa  # shortcut

# Check push status
git pushstatus
git ps  # shortcut

# Push and wait (blocking)
git pushasync --wait
```
