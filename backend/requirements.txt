# ═══════════════════════════════════════════════════════════════════════════════
# 🤿 DEEP SIGHT API v3.0 — Dependencies
# ═══════════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────────
# 🚀 CORE FRAMEWORK
# ─────────────────────────────────────────────────────────────────────────────────
fastapi>=0.109.1
uvicorn[standard]>=0.27.0
python-multipart>=0.0.22
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.0.0

# ─────────────────────────────────────────────────────────────────────────────────
# 🗃️ DATABASE
# ─────────────────────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.25
asyncpg>=0.29.0          # PostgreSQL async driver
aiosqlite>=0.19.0        # SQLite async driver (dev/fallback)
greenlet>=3.0.0          # Required for SQLAlchemy async

# ─────────────────────────────────────────────────────────────────────────────────
# 🔐 AUTHENTICATION
# ─────────────────────────────────────────────────────────────────────────────────
python-jose[cryptography]>=3.4.0
passlib>=1.7.4
bcrypt>=4.0.0            # Secure password hashing
authlib>=1.3.0           # OAuth (Google)
google-auth>=2.23.0      # Google ID token verification (mobile OAuth)

# ─────────────────────────────────────────────────────────────────────────────────
# 🌐 HTTP CLIENT
# ─────────────────────────────────────────────────────────────────────────────────
httpx[http2]>=0.26.0         # HTTP/2 support for better performance
aiohttp>=3.9.0

# ─────────────────────────────────────────────────────────────────────────────────
# 📺 YOUTUBE
# ─────────────────────────────────────────────────────────────────────────────────
youtube-transcript-api>=0.6.2
groq>=1.0.0

# ─────────────────────────────────────────────────────────────────────────────────
# 💳 PAYMENTS
# ─────────────────────────────────────────────────────────────────────────────────
stripe>=7.0.0

# ─────────────────────────────────────────────────────────────────────────────────
# 📧 EMAIL
# ─────────────────────────────────────────────────────────────────────────────────
resend>=2.30.0
aiolimiter>=1.1.0        # Async rate limiter for Resend API (prevents 429 errors)

# ─────────────────────────────────────────────────────────────────────────────────
# 📄 EXPORTS (PDF, DOCX, CSV, Excel)
# ─────────────────────────────────────────────────────────────────────────────────
python-docx>=1.1.0
reportlab>=4.0.0
markdown>=3.5.0
weasyprint>=60.0          # Beautiful HTML→PDF rendering
Jinja2>=3.1.0             # HTML templating for PDF exports
openpyxl>=3.1.0           # Excel export (.xlsx)

# ─────────────────────────────────────────────────────────────────────────────────
# 💾 BACKUP, MIGRATIONS & SCHEDULING
# ─────────────────────────────────────────────────────────────────────────────────
alembic>=1.13.0          # Database migrations
boto3>=1.34.0            # S3 backup storage
apscheduler>=3.10.0      # Cron scheduling (daily backups)
psycopg2-binary>=2.9.0   # Sync PostgreSQL driver (for SQL dumps)

# ─────────────────────────────────────────────────────────────────────────────────
# 💾 CACHE
# ─────────────────────────────────────────────────────────────────────────────────
redis>=5.0.0             # Redis async cache (optional, falls back to memory)
cachetools>=5.3.0        # In-memory TTL cache utilities

# ─────────────────────────────────────────────────────────────────────────────────
# 🛠️ UTILITIES
# ─────────────────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
aiofiles>=23.0.0         # Async file operations (TTS cache)
Pillow>=10.0.0           # Image processing (thumbnail WebP conversion)

# ─────────────────────────────────────────────────────────────────────────────────
# 🤖 AI MODELS
# ─────────────────────────────────────────────────────────────────────────────────
mistralai>=1.0.0

# ─────────────────────────────────────────────────────────────────────────────────
# 🎙️ AUDIO TRANSCRIPTION (optional - for Whisper fallback)
# ─────────────────────────────────────────────────────────────────────────────────
openai>=1.12.0                # OpenAI Whisper API
# assemblyai>=0.23.0          # AssemblyAI (uncomment if needed)

# ─────────────────────────────────────────────────────────────────────────────────
# 🤖 BOTS PROSPECTION B2B (Telegram + Luffa)
# ─────────────────────────────────────────────────────────────────────────────────
aiogram>=3.13.0,<4.0           # Telegram bot (async-first, parse Update, sendMessage)
luffa-bot-python-sdk>=0.1.2    # Luffa messaging platform (polling-based, official SDK)

# ─────────────────────────────────────────────────────────────────────────────────
# 🔍 MONITORING
# ─────────────────────────────────────────────────────────────────────────────────
sentry-sdk[fastapi]>=2.50.0
psutil>=5.9.0                # System metrics for health checks

# ─────────────────────────────────────────────────────────────────────────────────
# 🧪 TESTING (optionnel - pour développement)
# ─────────────────────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-timeout>=2.2.0
httpx>=0.26.0  # Pour TestClient FastAPI
fakeredis[lua]>=2.0  # Mock Redis async pour tests unit (tutor.service, etc.)
# youtube-search-python - removed (conflicts)
yt-dlp>=2024.1.0

# Note: Pour Nano Banana 2 (génération d'images), ajouter GEMINI_API_KEY dans les variables d'environnement
# L'API est appelée directement via httpx (déjà inclus)
