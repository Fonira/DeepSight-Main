# Tests Smoke (pytest)

Tests alternatifs en pytest. Les tests principaux sont dans `/monitoring/` (Playwright TypeScript).

Les workflows GitHub Actions (`smoke-tests.yml`, `smoke-on-deploy.yml`) utilisent la suite Playwright.
